import * as wired from './index';
import * as displayWired from '../display-wired-elements/index';

describe('wired-elements index', () => {
  it('exports WiredButton and other components', () => {
    expect(wired.WiredButton).toBeDefined();
    expect(wired.WiredCard).toBeDefined();
    expect(wired.WiredCalendar).toBeDefined();
    expect(wired.WiredCheckbox).toBeDefined();
    expect(wired.WiredCombo).toBeDefined();
    expect(wired.WiredDialog).toBeDefined();
    expect(wired.WiredDivider).toBeDefined();
    expect(wired.WiredFab).toBeDefined();
    expect(wired.WiredIconButton).toBeDefined();
    expect(wired.WiredImage).toBeDefined();
    expect(wired.WiredInput).toBeDefined();
    expect(wired.WiredLink).toBeDefined();
    expect(wired.WiredListbox).toBeDefined();
    expect(wired.WiredProgressRing).toBeDefined();
    expect(wired.WiredProgress).toBeDefined();
    expect(wired.WiredRadioGroup).toBeDefined();
    expect(wired.WiredRadio).toBeDefined();
    expect(wired.WiredSearchInput).toBeDefined();
    expect(wired.WiredSlider).toBeDefined();
    expect(wired.WiredSpinner).toBeDefined();
    expect(wired.WiredTabs).toBeDefined();
    expect(wired.WiredTextarea).toBeDefined();
    expect(wired.WiredToggle).toBeDefined();
    expect(wired.WiredVideo).toBeDefined();
  });
});

describe('display-wired-elements index', () => {
  it('exports WiredButton and other components', () => {
    expect(displayWired.WiredButton).toBeDefined();
    expect(displayWired.WiredCard).toBeDefined();
    expect(displayWired.WiredCalendar).toBeDefined();
    expect(displayWired.WiredCheckbox).toBeDefined();
    expect(displayWired.WiredCombo).toBeDefined();
    expect(displayWired.WiredDialog).toBeDefined();
    expect(displayWired.WiredDivider).toBeDefined();
    expect(displayWired.WiredFab).toBeDefined();
    expect(displayWired.WiredIconButton).toBeDefined();
    expect(displayWired.WiredImage).toBeDefined();
    expect(displayWired.WiredInput).toBeDefined();
    expect(displayWired.WiredLink).toBeDefined();
    expect(displayWired.WiredListbox).toBeDefined();
    expect(displayWired.WiredProgressRing).toBeDefined();
    expect(displayWired.WiredProgress).toBeDefined();
    expect(displayWired.WiredRadioGroup).toBeDefined();
    expect(displayWired.WiredRadio).toBeDefined();
    expect(displayWired.WiredSearchInput).toBeDefined();
    expect(displayWired.WiredSlider).toBeDefined();
    expect(displayWired.WiredSpinner).toBeDefined();
    expect(displayWired.WiredTabs).toBeDefined();
    expect(displayWired.WiredTextarea).toBeDefined();
    expect(displayWired.WiredToggle).toBeDefined();
    expect(displayWired.WiredVideo).toBeDefined();
  });
});
